# llm-line コア仕様（ドラフト）

**バージョン：** 0.1.0-draft  
**目的：** LLM 出力を安定して取り込み、ストリーミング処理を可能にする行指向フォーマットを定義する。

---

## 1. 概要
llm-line は最小限で人間にも読みやすい行指向フォーマットです。レコード、リスト、テキストブロックを明示的なマーカーで表現し、括弧や配列記号を排することで LLM の出力ミスによる致命的な構文エラーを減らします。

---

## 2. 基本概念
- **行（Line）**：最小単位。各要素は1行以上で表現される。  
- **レコード（Record）**：`@RECORD <name>` で開始するキー:値の集合。  
- **リスト（List）**：`@LIST <name>` で開始し、各項目は `- ` で始まる。  
- **テキストブロック（Text block）**：`@TEXT <name>` で開始し、複数行の自由テキストを含む。  
- **マーカー行**：`@` で始まる行は新しい要素の開始を示す。

---

## 3. 構文
- **レコード**
  ```
  @RECORD <name>
  key1: value1
  key2: value2
  ```
  - キーは英数字、`_`、`-` を許容。値は行末までの生テキスト。複数行値は TEXT を使用。

- **リスト**
  ```
  @LIST <name>
  - item1
  - item2
  ```

- **テキストブロック**
  ```
  @TEXT <name>
  複数行のテキスト
  がここに入る。
  ```

- **コメント / 空行**
  - 空行はトップレベル要素の区切り。  
  - `#` で始まる行はコメントとして無視。

---

## 4. エスケープと特殊文字
- 値に先頭 `- ` や `@` を含めたい場合は先頭に `\` を付ける。  
- エスケープは先頭文字にのみ適用される。

例：
```
@RECORD note
text: \@not-a-marker
item: \- not a list item
```

---

## 5. スキーマと検証
- スキーマは任意だが推奨。スキーマはレコード/リスト/テキスト名に対する期待フィールドと型を定義する。  
- パーサは中立的なイベントストリームを出力し、別プロセスでスキーマ検証を行う。  
- **未知フィールド** はデフォルトで保持される。検証ポリシーで拒否や変換を指定可能。

---

## 6. ストリーミングパーサの振る舞い（参照）
- パーサは行単位で読み、以下のイベントを出す：  
  - `start_record(name)`, `field(name, value)`, `end_record()`  
  - `start_list(name)`, `list_item(value)`, `end_list()`  
  - `start_text(name)`, `text_line(value)`, `end_text()`  
- ドキュメント全体をバッファしない設計。

---

## 7. エラー処理と堅牢性
- **寛容パース**：余分な空白や区切りの欠如などは WARN を出して継続。  
- **厳格モード**：CI 用に WARN を ERROR に昇格させる設定を用意。  
- **リカバリ**：解析不能行が出た場合、次の `@` マーカーで再同期を試みる。

---

## 8. 相互運用性
- **JSON ブリッジ**：レコード/リスト/テキストを JSON にマッピング可能。  
  - `@RECORD user` → `{ "user": { ... } }`  
  - `@LIST roles` → `{ "roles": [...] }`  
- **Markdown ブリッジ**：TEXT ブロックは Markdown セクションに変換可能。

---

## 9. バージョニングと互換性
- 仕様はセマンティックバージョニングを採用。  
- ファイルに `@SPEC-VERSION x.y.z` マーカーを付けて互換チェックを容易にする。

---

## 10. 例
`examples/` を参照。最小例：
```
@RECORD user
id: 1
name: Alice

@LIST tags
- beta
- test

@TEXT note
This is a note about Alice.
```

---

## 11. FAQ（短）
- **Q:** JSON を直せないのか？  
  **A:** JSON は厳格であり、LLM の小さな出力ズレで致命的になるため、取り込み耐性を高めるための実務的な代替として設計しています。  
- **Q:** JSON の代替か？  
  **A:** 置き換えではなく、取り込み用の実務フォーマットです。JSON との相互変換を想定しています。

---

## 12. フィードバック
ドラフトです。バグやエッジケース、採用事例は Issue でお知らせください。
